# Gemfileインストーラ
# インストールはターミナルでbundle installを実行して下さい。

# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# SQLite3データベースライブラリ
# SQLite3を使用して、一度問い合わせたデータを保管しておくことにより、再利用することで、動作の高速化、問い合わせ回数の減少に役立ちます。
gem "sqlite3"